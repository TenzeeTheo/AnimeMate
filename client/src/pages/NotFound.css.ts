import { style } from "@vanilla-extract/css";
import { vars } from "../styles/theme.css";

export const Nimg = style({
    display: "flex",
    width: "400px",
    margin: "0 auto",
})
export const con = style({
    background:vars.colors.favColor,
    width: "400px",
    margin: "0 auto",
    textAlign:"center"
})